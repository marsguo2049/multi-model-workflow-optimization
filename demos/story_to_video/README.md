# Story-to-Video demo

Run the packaged mock demo from the repository root:

```powershell
$env:PYTHONPATH = "src"
python demos/story_to_video/run.py "A small robot finds a garden beneath the city."
```

The output directory contains JSON records only. Connect real providers before expecting image or video media.
